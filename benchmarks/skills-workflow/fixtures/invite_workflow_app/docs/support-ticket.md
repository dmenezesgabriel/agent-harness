Customer reports that re-inviting a user after an old invite was archived still returns "Invite already exists".
Another request asks for the invite form and pending list to ship together as a thin vertical slice.
