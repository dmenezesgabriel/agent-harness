{
  "type": "object",
  "required": ["status", "checks_run", "findings", "recommendation"],
  "properties": {
    "status": {"type": "string", "enum": ["ready", "not_ready", "needs_issue_definition", "needs_implementation"]},
    "checks_run": {"type": "array", "items": {"type": "string"}},
    "findings": {"type": "array", "items": {"type": "string"}},
    "recommendation": {"type": "string"}
  }
}
