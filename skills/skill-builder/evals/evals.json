{
  "skill_name": "skill-builder",
  "evals": [
    {
      "id": "create-minimal-yaml-validator",
      "prompt": "Create an Agent Skill for validating YAML schema files before deployment. Keep it minimal — just the SKILL.md with a clear activation description, a basic workflow with a validation command, and at least one edge case.",
      "expected_output": "A valid skill directory containing SKILL.md with correct frontmatter, an activation-oriented description, an ordered workflow with a validation step, and at least one edge case.",
      "files": [],
      "tags": ["creation", "minimal"],
      "assertions": [
        {
          "text": "The output includes a SKILL.md file",
          "checks": [{"type": "file_exists", "path": "SKILL.md"}]
        },
        {
          "text": "The frontmatter contains a valid `name` field using only lowercase letters and hyphens",
          "checks": [{"type": "yaml_field", "path": "SKILL.md", "field": "name", "matches": "^[a-z0-9-]+$"}]
        },
        {
          "text": "The frontmatter contains a non-empty `description` field",
          "checks": [{"type": "yaml_field", "path": "SKILL.md", "field": "description", "min_length": 1}]
        },
        "The description is activation-oriented and says what the skill does and when to use it",
        {
          "text": "The body includes a `## Workflow` section with ordered steps",
          "checks": [{"type": "contains", "path": "SKILL.md", "text": "## Workflow"}]
        },
        "The workflow includes a validation command or checklist",
        {
          "text": "The body includes at least one edge case or gotcha",
          "checks": [{"type": "contains", "path": "SKILL.md", "text": "## Edge\n"}]
        },
        {
          "text": "The frontmatter parses as valid YAML",
          "checks": [{"type": "yaml_valid", "path": "SKILL.md"}]
        }
      ]
    },
    {
      "id": "fix-broken-frontmatter",
      "prompt": "This skill's SKILL.md has invalid frontmatter — the name has uppercase letters, and the description is empty. Fix it so it passes validation.",
      "expected_output": "A corrected SKILL.md with lowercase-hyphenated name matching the directory, and a non-empty activation-oriented description.",
      "files": ["evals/files/broken-frontmatter-skill/SKILL.md"],
      "tags": ["fix", "frontmatter", "validation"],
      "assertions": [
        {
          "text": "The `name` field uses only lowercase letters and hyphens",
          "checks": [{"type": "yaml_field", "path": "SKILL.md", "field": "name", "matches": "^[a-z0-9-]+$"}]
        },
        "The `name` field matches the parent directory name",
        {
          "text": "The `description` field is non-empty",
          "checks": [{"type": "yaml_field", "path": "SKILL.md", "field": "description", "min_length": 1}]
        },
        "The description says what the skill does and when to use it",
        {
          "text": "The frontmatter parses as valid YAML",
          "checks": [{"type": "yaml_valid", "path": "SKILL.md"}]
        },
        {
          "text": "The body is non-empty",
          "checks": [{"type": "not_empty", "path": "SKILL.md"}]
        }
      ]
    },
    {
      "id": "improve-vague-description",
      "prompt": "This skill's description is way too vague: \"Helps with files.\" Rewrite it to be activation-oriented, specific, and under 1024 characters. The skill is for a CSV quality reporter.",
      "expected_output": "A revised description that starts with activation-oriented phrasing, says what the skill does and when to use it, mentions concrete artifacts (CSV, quality checks), and is under 1024 characters.",
      "files": ["evals/files/vague-description-skill/SKILL.md"],
      "tags": ["description", "improvement"],
      "assertions": [
        "The description uses activation-oriented phrasing",
        "The description says what the skill does",
        "The description says when to use the skill",
        {
          "text": "The description mentions CSV or data quality",
          "checks": [{"type": "contains_regex", "path": "SKILL.md", "pattern": "CSV|data.quality"}]
        },
        {
          "text": "The description is under 1024 characters",
          "checks": [{"type": "char_count", "path": "SKILL.md", "field": "description", "max": 1024}]
        },
        {
          "text": "The original vague wording \"Helps with\" is replaced",
          "checks": [{"type": "not_contains", "path": "SKILL.md", "text": "Helps with"}]
        }
      ]
    }
  ]
}
